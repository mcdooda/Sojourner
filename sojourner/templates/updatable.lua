class.%s { extends('Updatable'),
  
  initialize = function(self, game)
    self.Updatable.initialize(self, game)
    -- initialize class members here --
    
    -----------------------------------
  end,
  
  update = function(self, input, key, time)
    -- update logic here --------------
    
    -----------------------------------
  end
  
}
